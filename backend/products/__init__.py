"""Products bounded context.

Lift A v2 — the per-workspace shipping unit (the persistence schema lives in
:mod:`backend.identity.workspaces_db` for v8 §5 reasons; the application
behaviours BSVibe adds on top — repo bootstrap, in particular — live here).
"""

from __future__ import annotations

__all__: list[str] = []
