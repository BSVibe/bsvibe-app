# BSVibe 세션 인수인계 — 2026-09-16

**prod**: `8a5a152` (코드는 `de60986`) · **열린 PR**: 이 문서 · **워크트리**: `wt/handoff-0916`
**워커**: 호스트 3개 — 배포마다 `launchctl kickstart -k gui/501/com.bsvibe.worker{,-admin,-mac-mini-e2e}`
**열린 작업은 이 문서가 아니라 GitHub 이슈에 있다.**

> 📦 직전 판들(`a121086` · `8a5a152` · `4091196` · `5ebfff1`)은 **Notion 아카이브로 아직 못 옮겼다**
> — 이 세션에도 Notion 도구가 없었다. 원문은 `git show <sha>:docs/HANDOFF.md`.

---

## §0 — 먼저 읽어라: **프레이밍 행은 아직 안 고쳐졌고, 이 세션의 가설 넷은 전부 틀렸다**

**prod 의 모든 런이 여전히 프레이밍에서 죽는다.** 이 세션은 코드를 못 고쳤다.
대신 **틀린 가설 넷을 실험으로 지웠다.** 전량은 **[#965 코멘트 2건](https://github.com/BSVibe/bsvibe-app/issues/965)**
— 첫 코멘트의 *"원인 확정(TCC)"* 은 **두 번째 코멘트가 반증한다. 첫 코멘트를 근거로 삼지 마라.**

### 지금 살아 있는 사실은 하나뿐이다

**`claude` 가 `openat$NOCANCEL` 에서 영원히 블록한다** (`sample` 결과 2243/2243 = 100%).
서브프로세스는 **정상 기동**하고(#968 마커가 1초 만에 찍힌다), 출력은 **단 한 줄도 없고**,
백엔드가 300초에 포기한다. **어느 경로를 여는지는 모른다.**

### 다음 사람이 할 단 하나의 측정

```
sudo fs_usage -w -f filesys        # 그리고 행을 재현한다
```

`tccd` 는 경로를 안 남기고 `fs_usage`/`dtruss` 는 root 가 필요하다. **이 경로를 얻기 전에는
어떤 원인 가설도 세우지 마라** — 이 이슈에서 그렇게 세운 가설이 지금까지 **넷 다 틀렸다.**

### 이 세션이 실험으로 지운 것

| 가설 | 어떻게 지웠나 |
|---|---|
| **TCC 동의 프롬프트가 pending 이라서** | `BSVIBE_WORKER_WORKSPACE_ROOT` 를 바꿔 **NetworkVolumes 요청이 0건**이 되게 만들었는데 **행은 그대로**였다 |
| **작업 디렉터리 위치(OS 임시 폴더)** | 같은 실험. cwd 를 형제 1개짜리 디렉터리로 옮겨도 행 |
| **"워커 안에서 100% 결정적"** | 09-15 12:11 프로세스에서 **12:32 성공 · 12:37 행이 동시에** 있었다 |
| **"TMPDIR 누수가 task 디렉터리"** | `bsvibe-task-*` = **0개**(cleanup 정상). 실제는 `bsvibe-shared-test-*` **17,721개** — **테스트 스위트 누수**(별개 이슈감) |

직전 판의 단서 A·B 도 반증됐다:
* **A (워커 재시작 직후 첫 태스크)** — 19시간 떠 있는 워커에서도 연속으로 행 났다
* **B (#970 커넥터 경고)** — **2.1초에 성공한 셸 replay 의 stderr 에도 그대로** 나왔다

### 누적 배제 목록 (다시 하지 마라)

CLI 자체 · 인증(만료 베어러) · `--model sonnet` · 프롬프트 바이트 replay · launchd 최소 env ·
stdin 미종료 · `MCP_CONNECTION_NONBLOCKING` · CLI 버전(9/12 이후 불변) · uv 버전(2월 이후 불변) ·
**행 난 자식의 env 전량 replay(2.1초 성공)** · **작업 디렉터리 위치** · **TCC**.

⇒ argv·env·cwd·TCC 전부 무죄. **셸에서 replay 하면 항상 성공하고, 워커 안에서만 재현된다.**

### ⚠️ prod 설정이 실험 상태로 남아 있다

세 워커 plist 에 **`BSVIBE_WORKER_WORKSPACE_ROOT=/Users/blasin/.bsvibe/worker-workspaces`** 가
들어가 있다. **행을 고치지 못했으므로 검증된 개선이 아니다.** 되돌리려면 plist 에서 그 키를 지우고
**bootout/bootstrap** 하라 — `kickstart -k` 는 plist 변경을 안 읽는다.

---

## §Ⅰ — 이 세션에 한 것

**코드 변경 0건.** 진단·반증·기록만 했다. 머지·배포 없음.

* #965 에 증거 코멘트 2건 (두 번째가 첫 번째를 정정한다)
* 이 문서

---

## §Ⅱ — 열린 이슈 (변동 없음)

`#970` · `#967` · `#965` · `#964` · `#961` · `#959` · `#957` · `#954` · `#951` · `#950` ·
`#949` · `#937` · `#936` · `#935` · `#930` · `#929` · `#928`

---

## §Ⅲ — 형님 손에 있는 것

1. **`sudo fs_usage` 1회** (§0) — 이 한 번이 조사를 끝낸다. 지금 최우선
2. **워커 Claude OAuth 재로그인** — `~/.bsvibe/claude_oauth.json` 이 2026-08-27 만료, refresh 가
   `invalid_grant`. 지금은 CLI 크리덴셜 폴백이 **유일한 생명줄**
3. 이월: **#935 시크릿 로테이션 2건** · **#937 리포 밖** · **Notion 아카이브**

> ~~TCC 전체 디스크 접근 권한~~ — **필요 없다. 실험으로 반증됐다.**

---

## §Ⅳ — 다음 세션 시작점

0. **§0 의 `fs_usage` 측정** — 그 전에 원인 가설을 세우지 마라
1. **#970 로그 소음** — 5분마다의 트레이스백을 한 줄로 줄이고 백오프. 30분짜리
2. **#957 KMS key-id** — 공개 가입이 우선순위를 올렸다
3. **#964 settle 스코프** — 국소적이고 구조가 이미 맞다
4. **#928 과금** — 여전히 단가 근거 없음. **#953 이전 숫자로 튜닝하지 마라**

### 검증 안 된 것 (정직하게)
* **#966 체크리스트 3번** — *긴 `act` 턴이 안 잘린다*를 **아직 못 봤다.** 프레이밍이 먼저 죽는다.
  **다음 성공 런에서 반드시** 확인할 것

---

## §Ⅴ — 이 세션에서 값을 한 규율

* **🚨 상관은 같은 초에 일어나도 인과가 아니다.** TCC 프롬프트가 `executor_turn_started` 와 **같은 초**에
  pending 이었고, 두 사건에서 반복됐다. 그래도 **틀렸다.** 확정은 *"그 조건을 없애면 증상이 사라지는가"*
  로만 온다 — 그 실험이 코드 0줄·10분이었는데 **나는 그걸 하기 전에 "원인 확정"이라고 썼다**
* **🚨 `log` 는 zsh 빌트인이다.** `log show …` 가 조용히 `too many arguments` 로 죽어 모든 TCC 쿼리가
  0줄을 반환했다. **양성 대조군(전체 줄 수)을 안 셌으면 반대로 오판**했다. `/usr/bin/log` 를 써라
* **🚨 `lsof -p PID -i` 는 AND 가 아니라 OR 다.** `-a` 없이 쓰면 **다른 프로세스의 소켓**이 섞인다
* **📉 TCC 로그는 Debug 레벨이라 이틀만 남는다.** 아카이브는 한 달치인데 그 subsystem 만 이틀이라,
  *"이전엔 프롬프트가 없었다"* 는 **보존 한계이지 관측이 아니다**
* **⏱ 타임아웃 난 프로세스는 죽기 전에 심문해라.** `lsof`·`sample`·`ps -Ewwo`(env 전량)는
  **살아 있는 동안에만** 얻어진다
* **🔬 "재현이 안 된다"는 재현 조건을 아직 못 베꼈다는 뜻** — 셸 replay 가 계속 성공한 건 argv·env 를
  베꼈지 **누가 띄웠는가**를 못 베꼈기 때문이다. **환경을 복사할 때 부모도 환경이다**
