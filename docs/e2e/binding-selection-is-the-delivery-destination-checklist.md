# E2E — 바인딩의 `selection` 이 배송 목적지다 (#1003 아웃바운드 축)

**대상 PR**: 디스패치가 `{**account.delivery_config, **binding.selection}` 를 쓴다
**세 축 전체와 승인 탭 결정**: `docs/architecture/telegram-group-chats.md`

> 📏 **이 변경은 오늘 prod 에서 no-op 이다.** 2026-09-22 실측, 살아 있는 바인딩 셋 전부
> `selection` 이 자기 계정의 `delivery_config` 와 값이 같다. **그래서 배포 후 체크리스트의
> 1순위는 "뭔가 달라졌나"가 아니라 "아무것도 안 달라졌나"** 다.

---

## 배포 전 — 실측 (완료)

- [x] **prod 바인딩 전수** (`bindings_list` + `connectors_list`, 읽기 전용)

      | 바인딩 `selection` | 계정 `delivery_config` | 같나 |
      |---|---|---|
      | `{"chat_id": "8242700007"}` | `{"chat_id": "8242700007"}` | ✅ |
      | `{"chat_id": "8242700007"}` | `{"chat_id": "8242700007"}` | ✅ |
      | `{"repo": "BSVibe/bsvibe-app", "base_branch": "main"}` | 동일 | ✅ |

      ⇒ **켜도 배송 한 건도 안 움직인다.** 이 사실은 `test_todays_prod_shape_is_a_no_op`
      에 테스트로 박아 뒀다 — 라이브 데이터에 대한 주장은 그게 거짓이 되면 빨개지는 곳에
      있어야 한다
- [x] 소비 지점을 **세었다**: 제네릭 경로에 둘(이벤트 빌더 · 플러그인 컨텍스트).
      github 둘은 `GithubBinding` 이라 **다른 메커니즘**이고 범위 밖
- [x] 전선 절단 **5건**, 전부 컴파일됐고(매번 **25개 실행**) 정확히 의도한 칸만 뒤집혔다

      | 절단 | 뒤집힌 것 |
      |---|---|
      | 병합 제거 | 목적지 테스트 2건 |
      | **빌더만** 병합 | **플러그인 컨텍스트 테스트 1건만** |
      | **컨텍스트만** 병합 | **빌더 테스트 1건만** |
      | 모호성 규칙 제거(첫 행 채택) | 불일치 테스트 1건 |
      | 리졸버가 `selection` 을 안 실음 | 6건 |

      가운데 둘이 이 PR 의 핵심이다 — 두 호출 지점이 **각각** 잡힌다
- [x] 패키지 LOC 가드를 **디렉터리 전수**로 바꿨다. 하드코딩 목록이라
      `_github_pr.py` · `_github_in_place.py` **두 파일이 이미 가드 밖**이었다(지금은 통과)

## 배포 후 — 걸 것

- [ ] **아무것도 안 달라졌는지부터** — 기존 제품으로 딜리버러블을 하나 내보내고
      **같은 방(`8242700007`)에 도착**하는지. 이게 no-op 주장의 유일한 prod 증거다
- [ ] 로그에 `connector_delivery_ambiguous_binding_selection` 이 **0건**인지.
      ⚠️ **0 만 보면 안 된다** — 같은 창에 `connector_delivery_dispatched` 가 있어야
      "배송이 실제로 흘렀는데 모호함이 없었다"가 된다(생산자 켜짐 확인)
- [ ] 🚨 **실제로 방을 나눠 본다** — 제품 하나의 바인딩 `selection.chat_id` 를 다른 방으로
      바꾸고(⚠️ **원래 값을 먼저 적어 두라**) 딜리버러블을 내보내 **새 방에 도착**하는지,
      그리고 **다른 제품은 원래 방 그대로**인지. 되돌린다
      *(못 하는 이유가 생기면 여기 적을 것 — 바인딩 수정은 설정 쓰기라 권한 분류기에
      막힐 수 있다)*
- [ ] 인바운드가 안 깨졌는지 — `selection` 은 `intake.py` 도 읽는다. 그룹/개인 메시지
      하나가 여전히 제품으로 들어오는지
