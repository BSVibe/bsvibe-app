services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    volumes:
      - ../..:/workspace:cached
    working_dir: /workspace
    command: sleep infinity
    networks:
      - default

  postgres:
    # pgvector/pgvector:pg16 = postgres:16 + pgvector extension preinstalled.
    # Required for `CREATE EXTENSION vector` in the 1.5b migration.
    image: pgvector/pgvector:pg16
    restart: unless-stopped
    environment:
      POSTGRES_USER: bsvibe
      POSTGRES_PASSWORD: bsvibe
      POSTGRES_DB: bsvibe
    ports:
      - "15442:5432"
    volumes:
      - devpgdata:/var/lib/postgresql/data
    networks:
      - default

  redis:
    image: redis:7-alpine
    restart: unless-stopped
    ports:
      - "16387:6379"
    networks:
      - default

volumes:
  devpgdata:

networks:
  default:
    name: bsvibe-app-devcontainer
